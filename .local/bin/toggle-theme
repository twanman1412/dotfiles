#!/usr/bin/env bash

STATE_FILE="$HOME/.cache/current_theme"
CURRENT=$(cat "$STATE_FILE" 2>/dev/null || echo "dark")

if [ "$CURRENT" = "dark" ]; then
    echo "light" > "$STATE_FILE"
else
    echo "dark" > "$STATE_FILE"
fi

"$HOME/.local/bin/apply-theme"
